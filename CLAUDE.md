# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — no source files, build configuration, or tests have been added yet. This file is a placeholder scaffold, not a description of an existing codebase.

Once real code lands, this file should be rewritten (not just appended to) to reflect what's actually here, including:

- **Build/lint/test commands** — the exact commands to build the project, run the linter, run the full test suite, and run a single test.
- **Architecture** — the high-level structure of the codebase: major modules/services, how they relate, and any non-obvious design decisions that span multiple files.
- **Conventions** — project-specific patterns worth calling out (e.g. a particular framework's idioms, a monorepo layout, a custom CLI).

Future Claude Code sessions: if you find this note still here alongside real project files, treat it as stale and regenerate this document from scratch based on what's actually in the repo (README, package manifests, CI config, existing rule files, etc.).
